package roborally.model;

public class Board{
	/*
	private final long width;
	private final long height;
	
	private final long UPPER_BOUND_WIDTH = Long.MAX_VALUE;
	private final long LOWER_BOUND_WIDTH = 0;
	private final long UPPER_BOUND_HEIGTH = Long.MAX_VALUE;
	private final long LOWER_BOUND_HEIGTH = 0;
	
	
	
	
	
	
	public boolean isValidHeight(long height){
		return height > LOWER_BOUND_HEIGHT && height <= UPPER_BOUND_HEIGHT;
	}
	
	public boolean isValidWidth(long width){
		return width > LOWER_BOUND_WIDTH && width <= UPPER_BOUND_WIDTH
	}
	*/
}
